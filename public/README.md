# Rent Myanmar Assets Folder

This folder serves static files directly at the root URL (e.g., `/logo.png`).

## How to use your custom logo:

1. Upload or place your custom logo image in this folder.
2. Name the file exactly **`logo.png`** (all lowercase).
3. The application will detect it automatically at runtime and replace the default vector logo with your custom file!
